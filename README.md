# rust-docker-boilerplate

## This project is an example of actix web framework.


### install docker on mac.

```shell
 brew install colima 
 colima start    
```

### init env file
```
cp .env.dist .env
```